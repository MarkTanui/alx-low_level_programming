#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints its name, followed by a new line.
 * @argc: counter
 * @argv: vector
 * Return: Always (0)
*/

int main(int argc, char *argv[])
{
	(void)argv;

	printf("%s\n", argv[0]);
	return (0);
}
